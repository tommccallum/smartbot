{
  "shell": "/usr/bin/mplayer -noautosub -novideo -noconsolecontrols %seek% -input file='%fifo%' -framedrop -vo null '%track%'",
  "commands": {
    "pause": "pause",
    "continue": "pause",
    "seek": "seek",
    "load": "loadfile"
  }
}
